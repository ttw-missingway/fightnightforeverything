# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw art staging. Sprite rips and portrait sources live here while they're
# being picked over; nothing in src/ reads from them. Keeping them out of the
# history means the repo doesn't carry megabytes of GIFs it never serves.
fighter-packs/
headshots/

# Dialogue generation scratch. The SPEC and the generator are the source of
# truth and are tracked; raw per-bucket output is a review artifact until a
# corpus is promoted into src/.
tools/dialogue/out/
